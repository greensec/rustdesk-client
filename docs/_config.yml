title: RustDesk Clients
description: Download customized RustDesk clients with pre-configured self-hosted server settings
baseurl: "/rustdesk-client"
url: "https://greensec.github.io"

plugins:
  - jekyll-feed

# Exclude these from the build
exclude:
  - Gemfile
  - Gemfile.lock
  - node_modules
  - vendor
